$(function(){
	
	
  $('#fileImageN').MultiFile({
    accept: 'gif|jpg|png',
    max: 10,
    STRING: { 
      remove:'Borrar', 
      selected:'Selecionado: $file', 
      denied:'Archivo invalido $ext!' 
    } 	
  });
  
    $('#fileImage').MultiFile({
    max: 1, 
    accept: 'gif|jpg|png',
    STRING: { 
      remove:'Borrar', 
      selected:'Selecionado: $file', 
      denied:'Archivo invalido $ext!' 
    } 	
  });
  
  
});